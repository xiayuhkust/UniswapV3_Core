[profile.default]
src = 'contracts'
out = 'out'
libs = ['lib']
solc = "0.7.6"
optimizer = true
optimizer_runs = 200

[rpc_endpoints]
local = "http://localhost:8545"

[etherscan]
mainnet = { key = "${ETHERSCAN_API_KEY}" }
